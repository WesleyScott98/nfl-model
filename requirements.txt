# Pinned deliberately: this file is installed in a CI job that has write access to the repo,
# so a compromised release of a transitive dependency could run code there. Bump on purpose.
nflreadpy==0.1.5
numpy==2.4.4
pandas==3.0.2
polars==1.44.2
pyarrow==25.0.1
requests==2.33.1
scipy==1.17.1
